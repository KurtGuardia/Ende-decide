'use client';
import Link from 'next/link'; import {useState} from 'react'; import {Shell,Card,PageTitle,Badge} from '@/components/ui'; import {getCommitmentStatus,useDemo} from '@/lib/demo-store';

export default function Tracking(){const {state,progress,evidence,escalate,close}=useDemo();const [modal,setModal]=useState('');const [message,setMessage]=useState('');const closed=state.closed;const status=getCommitmentStatus(state);const act=(f:()=>void,msg:string)=>{f();setModal('');setMessage(msg)};
 return <Shell><PageTitle title="Seguimiento del compromiso" subtitle="Control y trazabilidad del cumplimiento de la decisión." actions={<Badge kind={closed?'green':'amber'}>{status}</Badge>}/>
 <div className="hero"><Card><div className="decisioninfo"><div><b>DEC-2026-0042</b><h2 style={{margin:'4px 0'}}>Implementar siguiente etapa de generación renovable</h2><p>Comité ENDE Corporación<br/>Sesión del 11 de septiembre de 2026</p></div><div>Responsable<strong>María Fernández</strong><small>Gerencia de Generación</small><br/><br/>Comité<strong>Comité ENDE Corporación</strong></div><div>Fecha de decisión<strong>11 sep. 2026</strong><br/><br/>Fecha límite<strong>30 sep. 2026</strong></div></div></Card><Card title="Estado del compromiso"><div className="panel" style={{textAlign:'center'}}><span style={{fontSize:36}}>{closed?'✓':'◷'}</span><h2><Badge kind={closed?'green':'amber'}>{status}</Badge></h2><p>{closed?'Cierre validado; no se requieren acciones adicionales.':'Existe riesgo por actividades clave pendientes.'}</p></div></Card></div>
 <div className="grid twocol"><Card title="Avance e hitos del compromiso"><div className="panel"><div className="row" style={{justifyContent:'space-between'}}><b>Avance general</b><h2>{state.progress}%</h2></div><div className="progress" style={{width:'100%'}}><i style={{width:state.progress+'%'}}/></div><div className="row" style={{justifyContent:'space-between',marginTop:25,textAlign:'center'}}>{['✓ Decisión registrada','✓ Notificación enviada','✓ Avance reportado','✓ Evidencia cargada',closed?'✓ Validación completada':'○ Validación pendiente'].map((x,i)=><span key={x} className={i<4||closed?'check':''}>{x}<small style={{display:'block'}}> {11+i*3} sep. 2026</small></span>)}</div></div></Card><Card title="Semáforo y cumplimiento"><div className="panel"><div className="row"><span className="dot green"/> En plazo　{closed?1:0}</div><p className="row"><span className="dot amber"/> En riesgo　{closed?0:1}</p><div className="row"><span className="dot"/> Vencido　0</div><h1 style={{textAlign:'center'}}>{state.progress}%</h1></div></Card></div>
 <div className="grid threecol" style={{marginTop:12}}><Card title={closed?'Cierre y validación':'Alertas y recordatorios'}><div className="list">{(closed?['Cierre validado','Evidencia revisada','Compromiso cumplido']:['Riesgo de incumplimiento','Recordatorio de carga de evidencia','Recordatorio de avance','Notificación de decisión']).map((x,i)=><div className="listitem" key={x}><span className={'dot '+(closed?'green':i?'amber':'')}/><span><b>{x}</b><br/><small>{closed?'El registro fue consolidado en el expediente.':i?'Se solicita actualizar información del compromiso.':'El avance requiere atención prioritaria.'}</small></span></div>)}</div></Card><Card title="Última evidencia"><div className="panel">{state.evidence.length?state.evidence.slice(0,2).map(e=><div key={e.name} className="soft panel" style={{marginBottom:8}}><b>PDF　{e.name}</b><br/><small>Cargado el {e.date} · {e.author}</small><p>{e.note}</p></div>):<div className="empty">La evidencia se cargará durante el seguimiento.</div>}</div></Card><Card title="Bitácora de avances"><div className="timeline">{state.events.slice(0,6).map(e=><div className="event" key={e.id}><i className="dot"/><small>{e.date}</small><div><b>{e.title}</b><br/><small>{e.detail}</small></div></div>)}</div></Card><Card title="Acciones"><div className="actions">{closed?<><Link className="btn primary" href="/dashboard">Volver al Dashboard　→</Link><Link className="btn" href="/temas/DEC-2026-0042">Ver expediente　→</Link></>:<><button className="btn primary" onClick={()=>setModal('progress')}>▥　Registrar avance</button><button className="btn" onClick={()=>setModal('evidence')}>▤　Cargar evidencia</button><button className="btn danger" onClick={()=>setModal('escalate')}>↑　Escalar</button><button className="btn" onClick={()=>setModal('close')}>✓　Validar cierre</button></>}</div></Card></div>{message&&<div className="panel mint" style={{marginTop:12}}><b>✓ {message}</b></div>}{modal&&<Modal type={modal} dismiss={()=>setModal('')} done={(p?:number,n?:string)=>{if(modal==='progress')act(()=>progress(p||75,n||''),'Avance actualizado.');if(modal==='evidence')act(()=>evidence({name:n||'Evidencia_cumplimiento_septiembre.pdf',date:'08 sep. 2026',note:'Documentación de respaldo cargada durante la demo.',author:'María Fernández'}),'Evidencia cargada exitosamente.');if(modal==='escalate')act(escalate,'Compromiso escalado y registrado en bitácora.');if(modal==='close')act(close,'Cierre validado. El dashboard refleja el cumplimiento.')}}/>}</Shell>}

function Modal({type,dismiss,done}:{type:string;dismiss:()=>void;done:(p?:number,n?:string)=>void}){const[n,setN]=useState('');const[file,setFile]=useState('Evidencia_cumplimiento_septiembre.pdf');const[p,setP]=useState(75);const title=type==='progress'?'Registrar avance':type==='evidence'?'Cargar evidencia':type==='escalate'?'Escalar compromiso':'Validar cierre';return <div className="modalback"><div className="modal"><h2>{title}</h2>{type==='progress'&&<label>Porcentaje de avance<input type="number" min="0" max="99" value={p} onChange={e=>setP(+e.target.value)}/></label>}{type==='evidence'&&<label>Archivo simulado<input value={file} onChange={e=>setFile(e.target.value)}/></label>}{type!=='close'&&<label>Nota / observación<textarea value={n} onChange={e=>setN(e.target.value)} placeholder="Ingrese un comentario para la bitácora..."/></label>}<p>{type==='close'?'¿Confirma que la evidencia fue revisada y el compromiso está cumplido?':type==='escalate'?'Se creará una alerta prioritaria y un registro en el historial.':''}</p><div className="row"><button className="btn" onClick={dismiss}>Cancelar</button><button className={'btn '+(type==='escalate'?'danger':'primary')} onClick={()=>done(p,type==='evidence'?file:n)}>{type==='close'?'Confirmar validación':'Confirmar'}</button></div></div></div>}
